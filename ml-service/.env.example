UPLOAD_DIR=uploads
INTERNAL_API_KEY=change_this_to_a_long_random_shared_secret

# Set to "production" in production deployments. When set, INTERNAL_API_KEY
# above becomes required — the service will refuse to start with the
# insecure default shared secret.
ENVIRONMENT=development
